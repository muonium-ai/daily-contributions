---
id: T-000002
title: "Remove dead code in find_git_repos.py"
status: done
priority: p2
type: bug
effort: xs
labels: [code-review, cleanup]
tags: []
owner: null
created: "2026-03-13T00:00:00Z"
updated: "2026-03-13T00:00:00Z"
depends_on: []
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Remove redundant and dead code in the `os.walk` loop of `find_git_repos.py`.

## Acceptance Criteria
- [ ] Remove the redundant `if not is_ignored(root, ignore_paths)` guard on line 40 (the `continue` on line 38 already handles this)
- [ ] Remove the dead `if ".git" in dirs: dirs.remove(".git")` block on lines 42–43 (the dotfile filter on line 36 already strips `.git` from `dirs`)
- [ ] Script still correctly discovers all non-ignored git repos

## Notes
The dotfile filter `dirs[:] = [d for d in dirs if not d.startswith(".")]` already removes `.git` from `dirs`, and the `continue` earlier in the loop already skips ignored paths. Both later checks are unreachable/redundant.

## Implementation Plan
- [ ] Remove the inner `if not is_ignored(...)` wrapper — just keep `git_repos.append(root)` directly under `if has_git:`
- [ ] Remove the `if ".git" in dirs: dirs.remove(".git")` block
- [ ] Run `uv run find_git_repos.py` and verify output matches before/after
