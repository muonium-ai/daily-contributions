---
id: T-000032
title: "Rebrand project (new name TBD): rename package, scripts, docs, and references"
status: blocked
priority: p2
type: refactor
effort: m
labels: [refactor, rename, branding]
tags: []
owner: null
created: 2026-04-28T00:00:00Z
updated: 2026-04-28T00:00:00Z
depends_on: []
branch: null
retry_count: 0
retry_limit: 3
allocated_to: null
allocated_at: null
lease_expires_at: null
last_error: null
last_attempted_at: null
---

## Goal
Rename the project from `daily-contributions` to a name that better reflects its scope: tracking progress across repos AND performing repo-cleanup tasks for a CI server. Final name is **deferred** — this ticket stays `blocked` until the user picks one.

## Acceptance Criteria
- [ ] Final name chosen and recorded in this ticket's Notes section
- [ ] `pyproject.toml` `[project].name` updated; `[tool.hatch.build.targets.wheel].packages` updated
- [ ] `src/daily_contributions/` directory renamed to `src/<new_name>/`
- [ ] All `from daily_contributions...` / `import daily_contributions...` references updated across `tools/`, `tests/`, and any package-internal imports
- [ ] `Makefile` and `README.md` reference the new name (project name, install/run examples, project-structure tree)
- [ ] `uv lock` regenerated if needed; `uv run pytest tests/` is green
- [ ] `make report` produces the expected output end-to-end against the renamed package
- [ ] `git grep -i daily.contributions` returns no functional references (fixtures or archived ticket text are fine)

## Notes
**Name candidates discussed (not decided):** `groundskeeper`, `repo-steward`, `repo-warden`, `repo-pulse`, `devpulse`. User explicitly postponed the decision.

Scope expansion that motivated the rename: the project is moving beyond commit-activity reporting to also include cross-project progress views and CI-server-side repo cleanup utilities (e.g. `find_node_modules`, `image_sequence_detection`).

Keep the rebrand to a single ticket so git history shows one clean rename commit. The ticket prefix (`T-NNNNNN`) and `tickets/` flow stay as-is.

## Agent Assignment
- Suggested owner: claude
- Suggested branch: refactor/rebrand-<new_name>

## Implementation Plan
- [ ] Confirm the chosen name and slug (PyPI-style: lowercase, hyphenated)
- [ ] Update `pyproject.toml` (project name + hatch wheel package path)
- [ ] `git mv src/daily_contributions src/<new_name>` and update all imports
- [ ] Update `Makefile`, `README.md`, and any `tools/*.py` wrappers
- [ ] Run `uv sync`, `uv run pytest tests/`, `uv run ruff format --check .`, and `make report` end-to-end
